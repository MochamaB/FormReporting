@model FormReporting.Models.ViewModels.Forms.FormBuilderViewModel

@*
    CONFIGURATION TAB: Field-type specific settings
    - FormItemConfiguration table (Key-Value pairs)
    - minValue, maxValue, minLength, maxLength
    - allowedFileTypes, maxFileSize, ratingMax
    - dateFormat, allowMultiple, rows, cols, step
*@
