@model FormReporting.Models.ViewModels.Forms.SectionDto

@*
    BUILDER SECTION COMPONENT - SurveyJS Style
    Displays a section with transparent background and selection state
*@

<div class="card builder-section"
     data-section-id="@Model.SectionId"
     data-order="@Model.DisplayOrder"
     id="section-@Model.SectionId"
     onclick="selectSection(@Model.SectionId)">

    <!-- Section Header -->
    <div class="card-header d-flex align-items-center gap-2">
        <!-- Drag Handle -->
       

        <!-- Section Info -->
        <div class="flex-grow-1">
            <h5 class="card-title">@Model.SectionName</h5>
            @if (!string.IsNullOrEmpty(Model.SectionDescription))
            {
                <p class="text-muted mb-0 small">@Model.SectionDescription</p>
            }
        </div>

        <!-- Action Buttons -->
        <div class="d-flex gap-1" onclick="event.stopPropagation();">
        <!-- Drag Handle moved here -->
        <div class="drag-handle mr-2" style="border-right:2px solid #c6c6c6;">
            <i class="ri-draggable" style="font-size:1.5rem"></i>
        </div>
            <button type="button"
                    class="btn btn-secondary btn-sm"
                    onclick="duplicateSection(@Model.SectionId)"
                    title="Duplicate Section">
                <i class="ri-file-copy-line"></i>
                <span class="d-none d-md-inline ms-1"></span>
            </button>
            <button type="button"
                    class="btn btn-danger btn-sm"
                    onclick="deleteSection(@Model.SectionId)"
                    title="Delete Section">
                <i class="ri-delete-bin-line"></i>
                <span class="d-none d-md-inline ms-1"></span>
            </button>
            <button type="button"
                    class="btn btn-primary btn-sm"
                    onclick="toggleSectionCollapse(@Model.SectionId)"
                    title="Collapse/Expand">
                <i class="ri-subtract-line" id="collapse-icon-@Model.SectionId"></i>
            </button>
        </div>
    </div>

    <!-- Section Body (Collapsible) -->
    <div class="card-body" id="section-body-@Model.SectionId">
        <!-- Fields Container - Always render for drag-drop target -->
        <div class="fields-container"
             data-section-id="@Model.SectionId"
             data-column-layout="@Model.ColumnLayout"
             style="display: grid; grid-template-columns: repeat(@Model.ColumnLayout, 1fr); gap: 1rem;"
             onclick="event.stopPropagation();">
            @if (Model.Fields.Any())
            {
                @foreach (var field in Model.Fields.OrderBy(f => f.DisplayOrder))
                {
                    <!-- Use builder field preview component for WYSIWYG preview with builder UI -->
                    <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_BuilderFieldPreview.cshtml" model="@field" />
                }
            }
            else
            {
                @if (Model.ColumnLayout == 1)
                {
                    <div class="text-center text-muted py-3 empty-fields-message" style="grid-column: 1 / -1;">
                        <i class="ri-add-box-fill fs-4"></i>
                        <p class="mb-0 small">Drag fields here or click Add Question</p>
                    </div>
                }
                else if (Model.ColumnLayout == 2)
                {
                    <div class="text-center text-muted py-4 border border-dashed rounded empty-column-placeholder">
                        <i class="ri-layout-column-line fs-4 text-primary"></i>
                        <p class="mb-0 small">Column 1</p>
                        <small class="text-muted">Drag fields here</small>
                    </div>
                    <div class="text-center text-muted py-4 border border-dashed rounded empty-column-placeholder">
                        <i class="ri-layout-column-line fs-4 text-primary"></i>
                        <p class="mb-0 small">Column 2</p>
                        <small class="text-muted">Drag fields here</small>
                    </div>
                }
                else if (Model.ColumnLayout == 3)
                {
                    <div class="text-center text-muted py-4 border border-dashed rounded empty-column-placeholder">
                        <i class="ri-layout-grid-line fs-4 text-success"></i>
                        <p class="mb-0 small">Column 1</p>
                        <small class="text-muted">Drag fields here</small>
                    </div>
                    <div class="text-center text-muted py-4 border border-dashed rounded empty-column-placeholder">
                        <i class="ri-layout-grid-line fs-4 text-success"></i>
                        <p class="mb-0 small">Column 2</p>
                        <small class="text-muted">Drag fields here</small>
                    </div>
                    <div class="text-center text-muted py-4 border border-dashed rounded empty-column-placeholder">
                        <i class="ri-layout-grid-line fs-4 text-success"></i>
                        <p class="mb-0 small">Column 3</p>
                        <small class="text-muted">Drag fields here</small>
                    </div>
                }
            }
        </div>

        <!-- Add Question Dropdown -->
        <div class="text-center mt-3">
            @if (Model.ColumnLayout == 1)
            {
                <div class="dropdown" onclick="event.stopPropagation();">
                    <button class="btn btn-primary btn-lg w-100 dropdown-toggle"
                            type="button"
                            id="addQuestionDropdown-@Model.SectionId"
                            data-bs-toggle="dropdown"
                            aria-expanded="false">
                        <i class="ri-add-line me-1"></i>
                        Add Question
                    </button>
                    <ul class="dropdown-menu w-100"
                        aria-labelledby="addQuestionDropdown-@Model.SectionId"
                        style="max-height: 400px; overflow-y: auto;">
                        <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_FieldPaletteDropdown.cshtml"
                                 model="@Model.SectionId" />
                    </ul>
                </div>
            }
            else if (Model.ColumnLayout == 2)
            {
                <div class="row g-2">
                    <div class="col-6">
                        <div class="dropdown" onclick="event.stopPropagation();">
                            <button class="btn btn-outline-primary w-100 dropdown-toggle"
                                    type="button"
                                    id="addQuestionCol1-@Model.SectionId"
                                    data-bs-toggle="dropdown"
                                    data-column="1"
                                    aria-expanded="false">
                                <i class="ri-add-line me-1"></i>
                                Add to Column 1
                            </button>
                            <ul class="dropdown-menu w-100"
                                aria-labelledby="addQuestionCol1-@Model.SectionId"
                                style="max-height: 400px; overflow-y: auto;">
                                <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_FieldPaletteDropdown.cshtml"
                                         model="@Model.SectionId" />
                            </ul>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="dropdown" onclick="event.stopPropagation();">
                            <button class="btn btn-outline-primary w-100 dropdown-toggle"
                                    type="button"
                                    id="addQuestionCol2-@Model.SectionId"
                                    data-bs-toggle="dropdown"
                                    data-column="2"
                                    aria-expanded="false">
                                <i class="ri-add-line me-1"></i>
                                Add to Column 2
                            </button>
                            <ul class="dropdown-menu w-100"
                                aria-labelledby="addQuestionCol2-@Model.SectionId"
                                style="max-height: 400px; overflow-y: auto;">
                                <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_FieldPaletteDropdown.cshtml"
                                         model="@Model.SectionId" />
                            </ul>
                        </div>
                    </div>
                </div>
            }
            else if (Model.ColumnLayout == 3)
            {
                <div class="row g-2">
                    <div class="col-4">
                        <div class="dropdown" onclick="event.stopPropagation();">
                            <button class="btn btn-outline-primary btn-sm w-100 dropdown-toggle"
                                    type="button"
                                    id="addQuestionCol1-@Model.SectionId"
                                    data-bs-toggle="dropdown"
                                    data-column="1"
                                    aria-expanded="false">
                                <i class="ri-add-line me-1"></i>
                                Col 1
                            </button>
                            <ul class="dropdown-menu w-100"
                                aria-labelledby="addQuestionCol1-@Model.SectionId"
                                style="max-height: 400px; overflow-y: auto;">
                                <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_FieldPaletteDropdown.cshtml"
                                         model="@Model.SectionId" />
                            </ul>
                        </div>
                    </div>
                    <div class="col-4">
                        <div class="dropdown" onclick="event.stopPropagation();">
                            <button class="btn btn-outline-primary btn-sm w-100 dropdown-toggle"
                                    type="button"
                                    id="addQuestionCol2-@Model.SectionId"
                                    data-bs-toggle="dropdown"
                                    data-column="2"
                                    aria-expanded="false">
                                <i class="ri-add-line me-1"></i>
                                Col 2
                            </button>
                            <ul class="dropdown-menu w-100"
                                aria-labelledby="addQuestionCol2-@Model.SectionId"
                                style="max-height: 400px; overflow-y: auto;">
                                <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_FieldPaletteDropdown.cshtml"
                                         model="@Model.SectionId" />
                            </ul>
                        </div>
                    </div>
                    <div class="col-4">
                        <div class="dropdown" onclick="event.stopPropagation();">
                            <button class="btn btn-outline-primary btn-sm w-100 dropdown-toggle"
                                    type="button"
                                    id="addQuestionCol3-@Model.SectionId"
                                    data-bs-toggle="dropdown"
                                    data-column="3"
                                    aria-expanded="false">
                                <i class="ri-add-line me-1"></i>
                                Col 3
                            </button>
                            <ul class="dropdown-menu w-100"
                                aria-labelledby="addQuestionCol3-@Model.SectionId"
                                style="max-height: 400px; overflow-y: auto;">
                                <partial name="~/Views/Forms/FormTemplates/Partials/FormBuilder/Components/_FieldPaletteDropdown.cshtml"
                                         model="@Model.SectionId" />
                            </ul>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
