@*
    Duplicate Section Confirmation Modal
    Confirms user wants to duplicate a section before proceeding
*@

<!-- Duplicate Section Modal -->
<div class="modal fade" id="duplicateSectionModal" tabindex="-1" aria-labelledby="duplicateSectionModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary bg-gradient">
                <h5 class="modal-title text-white" id="duplicateSectionModalLabel">
                    <i class="ri-file-copy-line me-2"></i>Duplicate Section
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="text-center py-3">
                    <div class="mb-3">
                        <i class="ri-file-copy-2-line text-primary" style="font-size: 4rem;"></i>
                    </div>
                    <h5 class="mb-3">Duplicate section "<span id="duplicateSectionName" class="text-primary"></span>"?</h5>
                    <p class="text-muted mb-0">
                        This will create a copy with all fields.
                    </p>
                </div>

                <!-- Field Count Info -->
                <div id="duplicateInfoFields" class="alert alert-info d-none mt-3" role="alert">
                    <i class="ri-information-line me-1"></i>
                    This section contains <strong><span id="duplicateFieldCount">0</span> field(s)</strong> that will be copied to the new section.
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light" data-bs-dismiss="modal">
                    Cancel
                </button>
                <button type="button"
                        class="btn btn-primary"
                        id="confirmDuplicateBtn"
                        onclick="confirmDuplicateSection()">
                    <i class="ri-file-copy-line me-1"></i>OK
                </button>
            </div>
        </div>
    </div>
</div>
